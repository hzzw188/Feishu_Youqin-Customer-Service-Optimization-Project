# 优勤智服 · AI客服利润引擎 — 项目汇报文档

## 一、项目对三个核心问题的解决方法

### 问题一：客服效率低下 — 如何让 AI 真正替代重复性人工客服？

**问题本质：** 传统电商客服面临大量重复性咨询（产品参数、物流查询、优惠规则、安装指导等），人工客服逐一回复效率低、成本高，且无法 7×24 小时在线。

**解决方案：RAG 增强的 AI 大模型客服，三层安全机制保证可控**

1. **自建 RAG 知识库（62条结构化知识块）**
   - 覆盖品牌信息、10款商品详情、发货物流、退换货政策、优惠活动、FAQ 等全部领域
   - 采用字符级分词（unigram + bigram + trigram）+ 关键词加权检索，无需外部向量数据库
   - 知识库内容包含真实 SKU 数据、安装视频链接、国家标准编号等，确保回答准确可验证

2. **AI 大模型提示词工程（180行 System Prompt）**
   - 明确角色定义（优勤品牌 AI 客服）、品牌背景、核心能力
   - 9 组 Few-shot 示例覆盖产品咨询、投诉、优惠、物流、安装、推荐、差评威胁、商品对比、退款咨询等场景
   - 严格规定 `can_auto_answer` 判断标准：纯规则咨询可自动回答，投诉/退款/情绪激动不可自动回答
   - 强制 JSON 输出格式，确保后端可解析

3. **三层安全防御机制**
   - 第一层：AI 大模型提示词中的 `can_auto_answer` 规则
   - 第二层：后端 `ai.py` 硬编码关键词安全网（26 个投诉/催促关键词 + 负面情绪检测 + 风险等级检测），强制覆盖 AI 判断
   - 第三层：AI 大模型 API 不可用时的 `_fallback_analyze()` 降级模式（15 类意图匹配 + 预设话术模板）

4. **人机协同工作流**
   - AI 可自动回答的会话 → 自动生成回复 + 置信度标签，归入「AI接待」tab
   - 需人工处理的会话 → 自动移到「待接手」tab，生成 3 条推荐话术供客服参考
   - 工作台轮询 3 秒检测新消息，AI 思考动画展示检索→分析→生成全过程

**效果：** 约 50% 的客户咨询可由 AI 自动处理，客服仅需处理复杂投诉和售后问题，大幅提升人效。

---

### 问题二：客服价值无法量化 — 如何证明客服部门是利润中心而非成本中心？

**问题本质：** 传统客服部门被视为成本中心，其价值难以量化。售前转化了多少订单？售后挽回了多少退款？缺乏统一测算模型。

**解决方案：基于基准差额法的营收贡献测算模型**

1. **四维价值模型**（核心代码：[revenue_service.py](backend/app/services/revenue_service.py)）

| 价值类型 | 公式 | 触发条件 | 实现状态 |
|---------|------|---------|---------|
| 售前转化 Vconv | (1 − p) × GMV | 客户端点击「下单」按钮 | ✅ 已实现 |
| 退款挽回 Vretain | (q − r) × GMV | 售后客户端点击「已解决」按钮 | ✅ 已实现 |
| 复购增量 Vrep | 0 | 需 90 天消费跟踪 | 🔲 预留 |
| VOC 信息价值 Vvoc | 0 | 需工单确认流程 | 🔲 预留 |

2. **基准概率的规则化估算**（可解释、无需训练数据）
   - 基准成交概率 p：客户意图（+0.05~0.15）、情绪（+0.10）、风险（-0.10~-0.30）、客群标签（+0.05~0.15）加权
   - 基准退款概率 q：物流异常（+0.30）、大件组装商品（+0.15）加权
   - 接口设计预留替换为 ML 模型的空间

3. **客户端直接驱动贡献计算**
   - 售前会话页面提供「🛒 我下单了」按钮，点击后计算 Vconv
   - 售后会话页面提供「✅ 问题已解决」按钮，点击后计算 Vretain
   - 系统消息实时显示贡献值，客户端即时反馈

4. **驾驶舱全景展示**
   - 💰 客服总营收贡献：实时汇总所有会话的 Vconv + Vretain
   - 🤖 AI 贡献值：AI 自助接待会话的贡献总额及占比
   - AI vs 人工价值互补饼图：AI 转化/挽回 vs 人工转化/挽回四维拆解
   - 售前转化价值卡片：总额、会话数、均贡献、Top 5 转化明细
   - 归因明细表：每笔贡献可追溯到具体会话和事件

**效果：** 客服部门的价值首次以货币单位（¥）量化呈现，AI 贡献占比直观可见，为管理决策提供数据支撑。

---

### 问题三：多平台数据割裂 — 如何统一管理淘宝、京东、抖音、拼多多等多渠道客户？

**问题本质：** 电商客服需同时处理淘宝、天猫、京东、拼多多、抖音、快手、小红书、微信等多个平台的客户咨询，数据分散在各平台后台，无法统一管理和分析。

**解决方案：统一客服工作台 + 飞书多维表格数据中台**

1. **跨平台会话统一管理**
   - 工作台支持 10 个平台来源（淘宝、天猫、京东、拼多多、苏宁易购、唯品会、抖音、快手、小红书、微信）
   - 客户端（/server）提供独立的售前/售后咨询入口，自动关联平台和商品
   - 客户历史订单按平台生成，会话自动标注来源平台

2. **飞书多维表格 8 表数据中台**
   - 用户表、商品表、会话表、订单表、客服动作表、结果事件表、聊天消息表、KPI 驾驶舱指标表
   - 各表通过「会话ID」和「用户ID」关联，形成完整的数据血缘
   - 一键同步全部数据到飞书多维表格，支持增量同步和单表同步
   - 重建表结构采用「先改名后删」策略，绕过飞书限制

3. **驾驶舱跨平台数据聚合**
   - 自动聚合所有平台的会话、消息、订单数据
   - 营收贡献按平台、会话、事件类型多维度统计
   - 30 秒自动刷新，支持 4 种时间周期切换

**效果：** 一个工作台管理全部平台客户，一个驾驶舱查看全部平台数据，飞书多维表格实现数据沉淀和二次分析。

---

## 二、项目的创新点

### 创新点 1：客服营收贡献的货币化量化模型

- **首创性：** 将客服价值从"满意率""解决率"等模糊指标，转化为可计算的货币单位（¥），直接回答"客服赚了多少钱"的问题
- **科学性：** 采用基准差额法，通过对比"有客服介入"和"无客服介入（基准概率）"的差异，分离出客服的增量贡献
- **可解释性：** 基准概率基于规则权重模型（意图/情绪/风险/客群），无需黑盒 ML 模型，每个决策都可追溯解释
- **可扩展性：** 接口设计支持未来替换为 ML 模型，无需修改上下游代码

### 创新点 2：AI 与人工价值互补的可视化结构

- 驾驶舱新增「AI 与人工价值互补结构」环形饼图，将总营收贡献拆解为 AI 转化、AI 挽回、人工转化、人工挽回四个维度
- 直观展示 AI 在客服体系中的价值占比，为"AI 替代人工"的决策提供数据支撑
- 与「AI 贡献值」KPI 卡片和「归因明细表」形成三层数据联动

### 创新点 3：三层安全防御的 AI 可控机制

- 不同于简单的"AI 自动回答 + 人工兜底"，本项目设计了 Prompt 规则 → 关键词安全网 → 降级模板三层防御
- 安全网独立于 AI 模型运行，即使 AI 大模型误判投诉消息为"可自动回答"，后端硬编码规则也会强制覆盖
- 降级模式确保 AI 服务不可用时，系统仍能提供基础话术推荐

### 创新点 4：客户端驱动的贡献值触发机制

- 传统客服系统的价值评估是事后统计，本项目将贡献计算嵌入客户端交互流程
- 客户点击「下单」→ 实时计算转化贡献 → 系统消息反馈 → 驾驶舱 KPI 更新
- 售前售后分离，只有对应场景才触发对应价值计算，避免售前会话误算退款挽回

### 创新点 5：自建 RAG 知识库 — 轻量化、可解释、可审计

- **零外部依赖：** 62 条结构化知识块全部内嵌于代码中，无需向量数据库或外部检索服务，部署即用
- **字符级中文分词：** 采用 unigram + bigram + trigram 的组合分词策略，兼顾精确匹配和语义泛化，对中文电商场景（商品名、SKU、规格参数）检索效果优异
- **关键词加权检索：** 50% 关键词重叠度 + 30% 内容重叠度 + 20% 标题匹配奖励，多维度确保检索结果的相关性
- **可审计性：** 每条知识块可追溯来源（品牌方提供、商品详情页、售后政策文档），AI 回答基于明确的知识来源，而非黑盒生成
- **可替换性：** 知识库独立封装为 `knowledge_base.py` 模块，未来可替换为向量数据库（如 Milvus、Pinecone）或接入企业知识库平台，无需修改上层调用代码
- **领域深度：** 覆盖 10 款真实商品（含 SKU 编号、淘宝链接、安装视频 URL、国家标准编号）、品牌信息、物流政策、退换货规则、优惠活动、会员体系、FAQ 等 7 大类别 62 条知识

### 创新点 6：飞书多维表格 8 表数据中台

- 将会话、消息、订单、KPI 等全量数据自动同步到飞书多维表格
- 8 张表通过会话 ID 和用户 ID 关联，支持飞书内建的数据分析和可视化
- 重建表结构采用「先改名后删」策略，优雅解决飞书"至少保留 1 张表"的限制

---

## 三、项目结构技术特点

### 技术架构：前后端分离 + 云端部署

```
┌─────────────────────────────────────────────────────┐
│                   Cloudflare Pages                   │
│           (Vue 3 + Vite SPA 前端部署)                │
│          _redirects SPA路由 + _headers 安全头        │
└──────────────────────┬──────────────────────────────┘
                       │ HTTPS
                       ▼
┌─────────────────────────────────────────────────────┐
│                     Railway                         │
│              (FastAPI 后端部署)                       │
│     Nixpacks 构建 + Python 3.12 + SQLite            │
│     /api/health 健康检查 + ON_FAILURE 重启           │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│               AI 大模型 API (AI 服务)                 │
│         RAG 增强提示词 + JSON 结构化输出              │
│         降级模式：关键词匹配 + 预设话术               │
└─────────────────────────────────────────────────────┘
```

### 后端技术特点

| 特性 | 实现 |
|------|------|
| 框架 | FastAPI 0.115，自动生成 OpenAPI 文档 |
| ORM | SQLAlchemy 2.0，声明式模型 + 关系映射 |
| 数据库 | SQLite（零配置）+ 幂等迁移（`_ensure_columns` 自动补列） |
| AI 集成 | AI 大模型 API（兼容 OpenAI SDK）+ 自建 RAG 知识库 |
| 认证 | pbkdf2_hmac（SHA-256, 100k 迭代）+ 随机 salt |
| 物流 | 模拟物流状态机（pending→shipped→in_transit→delivering→delivered） |
| 飞书 | requests 直调飞书 OpenAPI，8 表并行同步 |
| 营收模型 | 基准差额法，规则权重估算，快照写入数据库 |
| 安全机制 | 三层防御（Prompt 规则 + 关键词安全网 + 降级模式） |

### 前端技术特点

| 特性 | 实现 |
|------|------|
| 框架 | Vue 3.5 + Composition API + TypeScript |
| 构建 | Vite 5.4，生产构建 ~770KB gzip |
| UI 库 | Element Plus 2.8（工作台 + 驾驶舱） |
| 图表 | ECharts 5.5（趋势图、饼图、直方图） |
| 状态管理 | Pinia 2.2，三层缓存策略（内存 Map + 60s TTL + 后台刷新） |
| 路由 | Vue Router 4.4，元信息守卫（public 路由免登录） |
| 数据流 | 乐观更新（Optimistic UI）+ 轮询 + 消息去重（Set<id>） |
| 跨会话安全 | 所有异步操作含 `stillCurrent` 会话 ID 检查 |
| 部署 | Cloudflare Pages（`_redirects` SPA 回退 + `_headers` 安全头） |

### 代码组织特点

- **服务层分离：** `revenue_service.py`（营收测算）、`ai_service.py`（AI 调用）、`knowledge_base.py`（RAG）、`logistics_service.py`（物流状态机）独立封装，可替换
- **路由模块化：** 8 个路由文件按功能域拆分（auth/sessions/ai/customer/cockpit/platforms/logistics/feishu）
- **接口契约统一：** Pydantic 模型定义请求/响应格式，前端 TypeScript 接口与后端一致
- **幂等迁移：** `_ensure_columns()` 方法自动检测并补全新增列，无需手动 ALTER TABLE
- **缓存 + 预取：** session store 实现多层缓存 + 相邻会话预取，切换会话秒出

---

## 四、项目与命题关系的理解

### 命题核心：AI 驱动的电商客服利润引擎

本项目围绕 **"让客服从成本中心转变为利润中心"** 这一核心命题展开，实现了三个层次的递进：

**第一层：AI 替代重复劳动（效率提升）**
- 通过 RAG + AI 大模型实现产品咨询、物流查询、优惠规则、安装指导等高频场景的自动回答
- 约 50% 的客户咨询可由 AI 自动处理，客服仅需处理复杂投诉
- 三层安全机制确保 AI 回答可控，不会在投诉场景下自动回复引发客诉升级

**第二层：客服价值量化（利润核算）**
- 基于基准差额法的营收贡献测算模型，将客服价值转化为货币单位（¥）
- 售前转化（Vconv）和退款挽回（Vretain）分别量化客服在两个核心场景的增量贡献
- 驾驶舱 KPI 卡片和 AI vs 人工价值饼图直观展示 AI 在客服体系中的价值占比

**第三层：数据中台打通（数据沉淀）**
- 飞书多维表格 8 表数据中台，将多平台、多会话的分散数据统一沉淀
- 8 张表通过会话 ID 和用户 ID 关联，支持飞书内建的数据分析
- 驾驶舱 30 秒实时刷新，归因明细可追溯到具体会话和事件

### 命题落地的技术支撑

| 命题要求 | 技术实现 |
|---------|---------|
| AI 智能客服 | AI 大模型 + 自建 RAG 知识库 + 三层安全防御 |
| 多平台统一管理 | 工作台支持 10 个平台来源 + 客户端独立入口 |
| 客服价值量化 | 基准差额法营收贡献模型 + 客户端下单/已解决按钮 |
| 数据可视化 | 驾驶舱 4 KPI + 3 图表 + 归因明细表 |
| 数据中台 | 飞书多维表格 8 表同步 + 会话 ID 关联 |
| 7×24 在线 | 乐观更新 + 3 秒轮询 + AI 思考动画 |
| 可信可控 | 置信度独立快照 + 强制人工安全网 + 降级模式 |

### 命题的深层理解

本项目的核心洞察是：**客服的价值不在于"接了多少电话"或"回复了多少消息"，而在于"促成了多少成交"和"挽回了多少退款"**。传统的客服 KPI（响应时间、满意度、解决率）是过程指标，而非结果指标。通过将客服价值与 GMV 直接挂钩，本项目实现了从"客服管理"到"客服经营"的范式转变。

同时，AI 的角色不是简单替代人工，而是与人工形成**价值互补结构**：AI 处理高频低风险的标准化咨询，人工处理低频高风险的复杂投诉。驾驶舱中的 AI vs 人工价值饼图正是这一理念的可视化呈现。

---

## 附录：关键文件索引

| 文件 | 说明 |
|------|------|
| [backend/app/services/revenue_service.py](backend/app/services/revenue_service.py) | 营收贡献测算核心模块 |
| [backend/app/services/knowledge_base.py](backend/app/services/knowledge_base.py) | 自建 RAG 知识库（62条，7大类别） |
| [backend/app/routers/ai.py](backend/app/routers/ai.py) | AI 分析 + 安全网络 + 模拟客户 |
| [backend/app/routers/customer.py](backend/app/routers/customer.py) | 客户端接口 + 下单/已解决 |
| [backend/app/routers/cockpit.py](backend/app/routers/cockpit.py) | 驾驶舱数据实时计算 |
| [frontend/src/views/Workbench.vue](frontend/src/views/Workbench.vue) | 客服工作台（三栏布局） |
| [frontend/src/views/Cockpit.vue](frontend/src/views/Cockpit.vue) | 利润驾驶舱（ECharts 图表） |
| [frontend/src/views/Server.vue](frontend/src/views/Server.vue) | 客户端（乐观更新 + 下单/已解决） |
| [frontend/src/stores/session.ts](frontend/src/stores/session.ts) | 会话状态管理（三层缓存） |
| [railway.json](railway.json) | Railway 后端部署配置 |
| [.github/workflows/deploy.yml](.github/workflows/deploy.yml) | Cloudflare Pages 前端部署工作流 |